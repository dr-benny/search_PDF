[phases.setup]
nixPkgs = ["nodejs", "npm", "python3", "python3-pip"]

[phases.install]
cmds = ["npm install", "pip3 install --break-system-packages -r requirements.txt"]

[start]
cmd = "node server.js"
